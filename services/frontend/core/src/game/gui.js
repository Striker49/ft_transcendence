import * as THREE from 'three'
import { GUI } from 'three/addons/libs/lil-gui.module.min.js';

export function setGui(paddleL, padleR) {
	const gui = new GUI();

	// gui.add(numberofWins, 1, 11).name('NumberofWins');

}